# Example environment file for the Bing grounding sample
# Copy this file to .env and fill in values.
# IMPORTANT: Do not commit secrets in .env.

# --- Foundry Project (required for smoke test) ---
# Preferred: full project endpoint
PROJECT_ENDPOINT=https://<your-foundry-account>.services.ai.azure.com/api/projects/<your-project>

# If your Foundry project has multiple model deployments, set this explicitly.
MODEL_DEPLOYMENT_NAME=<your-model-deployment>

# --- Bing Grounding connection reference (required unless skipping) ---
# The smoke test will resolve this name to a project-scoped connection id.
BING_GROUNDING_CONNECTION_NAME=binggrounding

# Workaround toggle: set to 1 only for troubleshooting.
BING_GROUNDING_USE_CONNECTION_NAME=0

# --- Auth (recommended) ---
# Use the Azure CLI identity from `az login`.
USE_AZURE_CLI_CREDENTIAL=1

# --- Optional behavior ---
# Set to 1 to skip the Bing-grounded portion.
SKIP_BING_GROUNDING=0

# Set to 1 only if you approve the runner to call `az` to discover Foundry account/project from AZURE_RESOURCE_GROUP.
ALLOW_AZ_DISCOVERY=0
AZURE_RESOURCE_GROUP=

# --- Connection creation helper (required only if you run create_bing_grounding_connection.py) ---
AZURE_SUBSCRIPTION_ID=<your-subscription-id>
FOUNDRY_RESOURCE_GROUP=<your-foundry-resource-group>
FOUNDRY_ACCOUNT_NAME=<your-foundry-account-name>
FOUNDRY_PROJECT_NAME=<your-foundry-project-name>

# Bing Grounding resource id (Microsoft.Bing/accounts). Example:
# /subscriptions/<sub>/resourceGroups/<rg>/providers/Microsoft.Bing/accounts/<bing-grounding-name>
BING_RESOURCE_ID=
